export const length = 10;
export const width = 5;

export function calculateArea(
  length: number,
  width: number
): number {

  return length * width;
}

export const result =
  calculateArea(length, width);